package com.vistoria.app.utils

// Simulação para evitar erro de compilação — ajustar se tiver lógica real
fun generatePdf(photos: List<String>) {
    for (photo in photos) {
        println("Adicionando foto no PDF: $photo")
    }
}
